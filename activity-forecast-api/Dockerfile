# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @collinson-test/weather-ranking-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @collinson-test/weather-ranking-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @collinson-test/weather-ranking-api && \
          adduser --system -G @collinson-test/weather-ranking-api @collinson-test/weather-ranking-api

COPY weather-ranking-api/dist @collinson-test/weather-ranking-api/
RUN chown -R @collinson-test/weather-ranking-api:@collinson-test/weather-ranking-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @collinson-test/weather-ranking-api --omit=dev -f install

CMD [ "node", "@collinson-test/weather-ranking-api" ]
